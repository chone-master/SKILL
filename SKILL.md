---
name: ask-clarify
description: Use when a developer needs iterative requirement clarification for feature work, secondary development, product alignment, or implementation planning.
---

# 需求澄清

帮用户把问题说清楚，尤其适合开发、二次开发、需求对齐和实现前澄清。核心原则：先找现有依据，再问用户决策，最后把共识沉淀成可复用文档。

## 工作流

1. 先重述当前需求，拆成用户目标、业务场景、输入输出、边界条件、验收标准。
2. 问问题前，先探索当前项目、产品或历史实现：代码、接口、配置、文档、测试、任务记录、PRD、相似页面或流程。
3. 把探索结果分成“已有类似处理”“仍需用户决策”“可能不在本次范围”。
4. 一次只问一个最高价值问题，说明为什么重要，给出建议答案，并明确这是建议。
5. 控制在 3 到 5 轮。信息足够时及时收敛，不急于给方案。
6. 达成共识后，写一份沉淀文档，保存需求背景、已确认结论、参考依据、待办边界、验收标准和未解决问题。

## 提问规则

- 一次只问一个问题，等待用户反馈后再继续；连续抛出多个问题会干扰判断。
- 不要把仓库、产品或历史资料能回答的问题丢给用户。
- 用户只负责回答意图、范围、优先级、风险偏好、兼容性取舍、验收口径等主观决策。
- 每个问题都要带上当前证据和建议答案：例如“我在 X 中看到类似流程是 Y，因此建议按 Z 处理；如果改成 A，代价是 B。”
- 如果没有必要继续追问，直接收敛成共识文档，不要为了追问而追问。

## 项目证据优先级

优先查找当前项目或产品中是否已有类似处理：

| 证据类型 | 该看什么 |
|---|---|
| 代码 | 相似功能、同名流程、状态机、枚举、DTO、Service、Controller、Mapper |
| 文档 | README、产品说明、PRD、Trellis 任务、接口文档、开发规范 |
| 测试与脚本 | 已有验收口径、边界用例、初始化数据、回归场景 |
| 历史记录 | Git diff、提交信息、任务记录、会话沉淀 |

## 沉淀文档

问完问题后，必须把共识写成文档。优先使用项目已有知识沉淀位置：

- 如果在 Trellis 任务中，更新当前任务的 `prd.md`；复杂需求再补 `design.md` 或 `implement.md`。
- 如果项目已有 `docs/`、`design/`、`需求说明` 等目录，按项目习惯新增或更新对应文档。
- 如果没有明显位置，先在当前任务目录或用户指定位置创建简短 Markdown 文档。

文档至少包含：

- 需求目标和用户价值
- 当前项目/产品中的类似处理及文件或文档依据
- 已确认的业务规则和边界
- 用户决策记录及取舍理由
- 可观察的验收标准
- 未解决问题和明确不做的范围

## 收敛输出

最终回复要简洁说明：

- 已查到哪些类似处理
- 已确认哪些需求结论
- 文档写到了哪里
- 还剩哪些阻塞问题，若没有就明确写“无阻塞问题”
